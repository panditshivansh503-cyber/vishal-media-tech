import { connectDB } from "@/lib/mongodb"; import { Service } from "@/models/Service";
export default async function ServicesAdmin(){let items:any[]=[];try{await connectDB();items=await Service.find().sort({createdAt:-1}).lean()}catch{}return <><div className="flex items-center justify-between"><h1 className="text-3xl font-black">Services</h1><button className="rounded-xl bg-brand-red px-5 py-3 font-bold text-white">Add Service</button></div><div className="mt-7 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{items.map(s=><div key={String(s._id)} className="rounded-2xl bg-white p-6 shadow-sm"><h3 className="font-black">{s.title}</h3><p className="mt-2 text-sm text-gray-500">{s.description}</p></div>)}{items.length===0&&<div className="rounded-2xl bg-white p-8 text-gray-500">No custom services yet. Seed or create them through the service API.</div>}</div></>}
