import { connectDB } from "@/lib/mongodb"; import { Inquiry } from "@/models/Inquiry";
export default async function Leads(){let leads:any[]=[];try{await connectDB();leads=await Inquiry.find().sort({createdAt:-1}).lean()}catch{}return <><h1 className="text-3xl font-black">Lead Management</h1><p className="mt-1 text-gray-500">All website enquiries in one place.</p><div className="mt-7 overflow-hidden rounded-2xl bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-sm"><thead className="bg-gray-50"><tr><th className="p-4">Name</th><th>Contact</th><th>Service</th><th>Status</th><th>Created</th></tr></thead><tbody>{leads.map(l=><tr key={String(l._id)} className="border-t"><td className="p-4 font-bold">{l.fullName}<div className="font-normal text-gray-500">{l.companyName}</div></td><td>{l.mobile}<div className="text-gray-500">{l.email}</div></td><td>{l.serviceRequired}</td><td><span className="rounded-full bg-red-50 px-3 py-1 text-xs font-bold text-brand-red">{l.status}</span></td><td>{new Date(l.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table>{leads.length===0&&<div className="p-10 text-center text-gray-500">No leads yet.</div>}</div></div></>}
