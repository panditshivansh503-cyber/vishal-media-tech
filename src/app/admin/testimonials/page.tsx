import { connectDB } from "@/lib/mongodb"; import { Testimonial } from "@/models/Testimonial";
export default async function TestimonialsAdmin(){let items:any[]=[];try{await connectDB();items=await Testimonial.find().sort({createdAt:-1}).lean()}catch{}return <><div className="flex items-center justify-between"><h1 className="text-3xl font-black">Testimonials</h1><button className="rounded-xl bg-brand-red px-5 py-3 font-bold text-white">Add Testimonial</button></div><div className="mt-7 grid gap-4 md:grid-cols-2">{items.map(t=><div key={String(t._id)} className="rounded-2xl bg-white p-6 shadow-sm"><div className="text-yellow-500">{"★".repeat(t.rating||5)}</div><p className="mt-3">“{t.review}”</p><p className="mt-4 font-black">{t.name}</p><p className="text-sm text-gray-500">{t.company}</p></div>)}</div></>}
