import { connectDB } from "@/lib/mongodb"; import { Inquiry } from "@/models/Inquiry"; import { Client } from "@/models/Client";
export default async function Dashboard(){let leads=0,clients=0,newLeads=0,converted=0;try{await connectDB();[leads,clients,newLeads,converted]=await Promise.all([Inquiry.countDocuments(),Client.countDocuments(),Inquiry.countDocuments({status:"New"}),Inquiry.countDocuments({status:"Converted"})])}catch{}const cards=[["Total Leads",leads],["New Leads",newLeads],["Converted",converted],["Active Clients",clients]];return <><div className="flex items-end justify-between"><div><p className="text-sm font-semibold text-brand-red">OVERVIEW</p><h1 className="text-3xl font-black">Dashboard</h1></div><div className="rounded-xl bg-white px-4 py-2 text-sm shadow-sm">Live CRM</div></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([n,v])=><div key={n} className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm text-gray-500">{n}</p><p className="mt-2 text-4xl font-black">{v}</p></div>)}</div><div className="mt-7 grid gap-6 lg:grid-cols-2"><div className="rounded-2xl bg-white p-6"><h2 className="font-black">Lead Growth</h2><div className="mt-8 flex h-64 items-end gap-3">{[30,48,42,65,54,82,96,72,110,98,130,145].map((x,i)=><div key={i} className="flex-1 rounded-t-lg bg-brand-red/80" style={{height:`${Math.max(8,x/1.6)}%`}}/>)}</div></div><div className="rounded-2xl bg-white p-6"><h2 className="font-black">Conversion Rate</h2><div className="mt-8 flex items-center justify-center"><div className="flex h-52 w-52 items-center justify-center rounded-full border-[28px] border-brand-red"><div className="text-center"><b className="text-4xl">{leads?Math.round(converted/leads*100):0}%</b><p className="text-xs text-gray-500">converted</p></div></div></div></div></div></>}
