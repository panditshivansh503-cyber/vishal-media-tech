import { connectDB } from "@/lib/mongodb"; import { Portfolio } from "@/models/Portfolio";
export default async function PortfolioAdmin(){let items:any[]=[];try{await connectDB();items=await Portfolio.find().sort({createdAt:-1}).lean()}catch{}return <><div className="flex items-center justify-between"><h1 className="text-3xl font-black">Portfolio</h1><button className="rounded-xl bg-brand-red px-5 py-3 font-bold text-white">Add Project</button></div><div className="mt-7 grid gap-4 md:grid-cols-2">{items.map(p=><div key={String(p._id)} className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-xs font-bold text-brand-red">{p.category}</p><h3 className="mt-1 text-xl font-black">{p.clientName}</h3><p className="mt-2 text-gray-500">{p.industry}</p><p className="mt-4 font-bold">{p.result}</p></div>)}</div></>}
