 "use client";
import { useForm } from "react-hook-form"; import { zodResolver } from "@hookform/resolvers/zod"; import { loginSchema } from "@/lib/validators"; import { useRouter } from "next/navigation"; import { toast } from "sonner";
type Form={email:string;password:string};
export default function Login(){const {register,handleSubmit,formState:{errors}}=useForm<Form>({resolver:zodResolver(loginSchema)});const router=useRouter();const submit=async(d:Form)=>{const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(d)});if(!r.ok)return toast.error("Invalid credentials");toast.success("Welcome back");router.push("/admin")};return <main className="flex min-h-screen items-center justify-center bg-gray-950 p-5"><form onSubmit={handleSubmit(submit)} className="w-full max-w-md rounded-3xl bg-white p-8"><div className="text-2xl font-black"><span className="text-brand-red">Vishal</span> Media Tech</div><p className="mt-2 text-gray-500">Secure admin access</p><label className="mt-8 block text-sm font-semibold">Email<input {...register("email")} className="mt-2 w-full rounded-xl border p-3.5"/>{errors.email&&<span className="text-xs text-red-600">{errors.email.message}</span>}</label><label className="mt-5 block text-sm font-semibold">Password<input type="password" {...register("password")} className="mt-2 w-full rounded-xl border p-3.5"/>{errors.password&&<span className="text-xs text-red-600">{errors.password.message}</span>}</label><button className="mt-7 w-full rounded-xl bg-brand-red py-4 font-bold text-white">Login</button></form></main>}
